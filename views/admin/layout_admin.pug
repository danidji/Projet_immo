doctype html
html
    head
        title= title
        link(rel='stylesheet', href='/stylesheets/style.css')
    body
      .grid
        .headTitle
          h1 TeLoger
        .head_admin
          .admin 
              h1 Admin 
          .notif 
              .notif_msg 
                a(class='msg',href='/admin/messages') ✉️ Messages
                span.nb.active #{session.nbMsg}
                  
              //- .notif_alert ALT
        .sideBar_admin
          .userData 
              .photo 
              .name #{user.firstname} #{user.lastname}
              .status 🟢 online 
          .menu_admin 
              ul
                each val in [{lien:'/admin/dashboard',text:'🖥️ Dashboard'},{lien:'/admin/realtyList',text:'🏠 Biens'},{lien:'/admin/users',text:'👥 Utilisateurs'},{lien:'/admin/logout_admin',text:'📴 Logout'}]
                  li
                    a(class=val.class,href=val.lien) #{val.text}

        .section_admin
          //- - var messages = getMessages()
          //-   if messages.notify
          //-     each msg in messages.notify
          //-       .alert.alert-info= msg
          //-   if messages.error
          //-     each msg in messages.error
          //-       .alert.alert-danger= msg
          
          block content
        
    block scripts 
      script(src="/javascripts/admin/upload.js")
